C appendixC
{ [x] = 0; [y] = 0; [p] = 0; [q] = 0; }

P0 (volatile int* p, volatile int* q) {
  int t = *p;
  *q = 1;
  if (t) {
    atomic_store_explicit(y, 1, memory_order_relaxed);
  }
}

P1 (volatile int* p, volatile int* q) {
  if (*q) {
    *p = 1;
    atomic_store_explicit(x, 1, memory_order_relaxed);
  }
}

exists (p=1 /\ q=1)
