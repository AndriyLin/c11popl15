C A2reordered
{ [x] = 0; [y] = 0; [z] = 0; }

P0 (volatile int* x, volatile int* y) {
  int r0 = SCAS(x, z, 1, memory_order_release, memory_order_relaxed);
  int r1 = atomic_load_explicit(y, memory_order_relaxed);
}

P1 (volatile int* x, volatile int* y) {
  int r2 = atomic_load_explicit(x, memory_order_acquire);
  if (r2) {
    *y = 1;
  }
}
